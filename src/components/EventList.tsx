"use client";

import { useMemo, useState } from "react";
import type { Event } from "@/types/database";

const statusStyles: Record<string, string> = { pending: "bg-amber-100 text-amber-700", approved: "bg-blue-100 text-blue-700", preparing: "bg-purple-100 text-purple-700", deployed: "bg-indigo-100 text-indigo-700", completed: "bg-emerald-100 text-emerald-700", cancelled: "bg-slate-200 text-slate-600" };
const label = (value: string) => value.replaceAll("_", " ");
const dateTime = (value: string) => new Intl.DateTimeFormat("en-MY", { day: "2-digit", month: "short", year: "numeric", hour: "2-digit", minute: "2-digit" }).format(new Date(value));

function EventCard({ event }: { event: Event }) {
  return <article className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-4"><div><p className="text-sm font-semibold text-blue-600">{dateTime(event.event_start_time)}</p><h2 className="mt-1 text-lg font-bold text-slate-900">{event.event_name}</h2></div><span className={`rounded-full px-2.5 py-1 text-xs font-semibold capitalize ${statusStyles[event.status] ?? "bg-slate-100 text-slate-700"}`}>{label(event.status)}</span></div><dl className="mt-5 space-y-3 text-sm"><div className="flex justify-between gap-4"><dt className="text-slate-500">PIC</dt><dd className="text-right font-medium text-slate-800">{event.pic?.name ?? "Not assigned"}</dd></div><div className="flex justify-between gap-4 border-t border-slate-100 pt-3"><dt className="text-slate-500">Location</dt><dd className="text-right font-medium text-slate-800">{event.location?.name ?? "Not assigned"}</dd></div>{event.event_description && <div className="border-t border-slate-100 pt-3 text-slate-600">{event.event_description}</div>}</dl></article>;
}

export default function EventList({ events }: { events: Event[] }) {
  const [search, setSearch] = useState("");
  const [status, setStatus] = useState("all");
  const [period, setPeriod] = useState("upcoming");
  const statuses = useMemo(() => [...new Set(events.map((event) => event.status))].sort(), [events]);
  const [now] = useState(() => Date.now());
  const results = useMemo(() => { const term = search.trim().toLowerCase(); return events.filter((event) => [event.event_name, event.event_description, event.pic?.name, event.location?.name].filter(Boolean).join(" ").toLowerCase().includes(term) && (status === "all" || event.status === status) && (period === "all" || (period === "upcoming" ? new Date(event.event_start_time).getTime() >= now : new Date(event.event_start_time).getTime() < now))).sort((a, b) => new Date(a.event_start_time).getTime() - new Date(b.event_start_time).getTime()); }, [events, now, period, search, status]);
  const upcoming = events.filter((event) => new Date(event.event_start_time).getTime() >= now && event.status !== "cancelled").length;
  const active = events.filter((event) => ["approved", "preparing", "deployed"].includes(event.status)).length;

  return <section className="mt-8"><div className="grid gap-3 sm:grid-cols-3">{[["Total events", events.length], ["Upcoming", upcoming], ["Active", active]].map(([name, value]) => <div key={name} className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><p className="text-sm text-slate-500">{name}</p><p className="mt-1 text-2xl font-bold text-slate-900">{value}</p></div>)}</div><div className="mt-6 grid gap-3 rounded-xl border border-slate-200 bg-white p-4 shadow-sm md:grid-cols-[1fr_14rem_14rem]"><input value={search} onChange={(event) => setSearch(event.target.value)} aria-label="Search events" placeholder="Search event, PIC, location..." className="rounded-lg border border-slate-300 px-3 py-2 text-sm" /><select value={status} onChange={(event) => setStatus(event.target.value)} aria-label="Filter by event status" className="rounded-lg border border-slate-300 px-3 py-2 text-sm"><option value="all">All statuses</option>{statuses.map((value) => <option key={value} value={value}>{label(value)}</option>)}</select><select value={period} onChange={(event) => setPeriod(event.target.value)} aria-label="Filter by event period" className="rounded-lg border border-slate-300 px-3 py-2 text-sm"><option value="upcoming">Upcoming</option><option value="past">Past</option><option value="all">All dates</option></select></div><p className="mt-4 text-sm text-slate-500">Showing {results.length} of {events.length}</p>{results.length ? <><div className="mt-4 grid gap-4 md:hidden">{results.map((event) => <EventCard key={event.id} event={event} />)}</div><div className="mt-4 hidden overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm md:block"><div className="overflow-x-auto"><table className="w-full min-w-[800px] text-left text-sm"><thead className="border-b border-slate-200 bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-3">Event</th><th className="px-5 py-3">Date & time</th><th className="px-5 py-3">PIC</th><th className="px-5 py-3">Location</th><th className="px-5 py-3">Status</th></tr></thead><tbody className="divide-y divide-slate-100">{results.map((event) => <tr key={event.id} className="hover:bg-slate-50"><td className="px-5 py-4"><span className="block font-semibold text-slate-900">{event.event_name}</span><span className="mt-0.5 block max-w-64 truncate text-xs text-slate-500">{event.event_description || "No description"}</span></td><td className="whitespace-nowrap px-5 py-4 text-slate-700">{dateTime(event.event_start_time)}</td><td className="px-5 py-4 text-slate-700">{event.pic?.name ?? "Not assigned"}</td><td className="px-5 py-4 text-slate-700">{event.location?.name ?? "Not assigned"}</td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold capitalize ${statusStyles[event.status] ?? "bg-slate-100 text-slate-700"}`}>{label(event.status)}</span></td></tr>)}</tbody></table></div></div></> : <div className="mt-4 rounded-xl border border-dashed border-slate-300 bg-white p-10 text-center"><h2 className="font-semibold text-slate-800">No matching events</h2><p className="mt-2 text-sm text-slate-500">Try another date range, status, or search.</p></div>}</section>;
}
